# Hub DSR Excel Template

Place the official workbook provided by the Hub PMO in this folder and name it `Hub_DSR_Template.xlsx`.

The backend (ExcelJS) clones this template for every export to preserve column widths, merged headers, and brand colors.

If the file is missing at runtime, the API falls back to a simplified sheet, but leadership deliverables may not match the mandated format.
